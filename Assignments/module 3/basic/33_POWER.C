// PROGRAM 33 : READ INTEGER AND PRINT 1ST THEREE POWER (N^1, N^2, N^3) - (START) 
#include<stdio.h>
main(){
    int n, power, next;

    printf("Enter number : ");
    scanf("%d", &n);

    printf("%d, %d, %d", n, n*n, n*n*n);
}
// PROGRAM 33 : READ INTEGER AND PRINT 1ST THEREE POWER (N^1, N^2, N^3) - (START) 
